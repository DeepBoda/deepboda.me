# Netlify config for deepboda.in
#
# The site is a static export. Everything it loads is self hosted, the only
# external references are outbound links, so the CSP can be strict.

[build]
  command = "npm run build"
  publish = "out"

# ===========================================================================
# Everything BELOW this line is copied into out/ by scripts/finish-export.mjs,
# so a drag-and-drop deploy keeps the headers and redirects. The [build] block
# above must NOT go with it: out/ has no package.json, so Netlify would try to
# build an already-built folder and fail with ENOENT.
# ===========================================================================

# ---------------------------------------------------------------------------
# Redirects
# ---------------------------------------------------------------------------

# The netlify.app address serves the same pages as the custom domain, which is
# duplicate content and splits any ranking signal between two hostnames. Send
# it to the real domain permanently.
[[redirects]]
  from = "https://deepboda.netlify.app/*"
  to = "https://deepboda.in/:splat"
  status = 301
  force = true

# ---------------------------------------------------------------------------
# Headers
# ---------------------------------------------------------------------------

[[headers]]
  for = "/*"
  [headers.values]
    # one year, and ask to be added to the browser preload list
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=(), interest-cohort=()"
    # 'unsafe-inline' on style is required: Next inlines critical CSS and the
    # components set CSS custom properties through the style attribute.
    # script-src needs 'unsafe-inline'. Next emits an inline bootstrap script
    # on every page to hydrate the server-rendered HTML, and its content is
    # different per page and changes on every rebuild, so neither a nonce
    # (needs a live server to mint one per request; this is a static export
    # with none) nor a fixed hash (would need to be recomputed and pinned for
    # every one of 21 pages, every build) can pin it down. Blocking it does
    # not just lose an inline script: it fails hydration outright, so React
    # never attaches to the page and every client feature on the site (nav,
    # ScrollTrigger, the hamburger, reveal animations) goes dead. Verified
    # live: deepboda.in threw CSP violations plus React error #412 (hydration
    # mismatch) on this exact rule before the fix. There is no user input and
    # no third-party content on this site, so the realistic downside of
    # allowing inline scripts here is low.
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'; upgrade-insecure-requests"

# Hashed build assets never change under the same name, so cache them hard.
[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.woff2"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# The CV is replaced in place, so it must not be cached for a year.
[[headers]]
  for = "/Deep-Boda-DevOps-Engineer-CV.pdf"
  [headers.values]
    Cache-Control = "public, max-age=3600, must-revalidate"
